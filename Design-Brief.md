# 设计规范：印尼 Payroll Agent 交付系统

文档状态：v1.2 已同步 Product Spec 的 AI Intake、ChangeProposal/ChangeLedger、Customer Confirmation Pack
生成日期：2026-06-15
事实来源：`Product-Spec.md`、`Product-Spec-CHANGELOG.md`、`DEV-PLAN.md`、已有 Next.js 骨架、用户补充“这是一个高责任 Agent 产品设计”、用户确认 V1 只做 PC 端
外部参考：ADP Global Payroll、Deel Payroll、Workday Payroll、OpenAI Agents guardrails/human review、PwC Payroll Controls for Workday
高保真设计参考：`docs/design-references/IDNPY-CPV2/Payroll Agent.dc.html`

## 0. AI 使用说明

- 本文档定义 V1 的界面结构、视觉系统、组件、状态、交互规则和嵌入式高责任 Agent 呈现方式。
- `Product-Spec.md` 是功能范围的事实来源；本文档不得新增 Product Spec 没有定义的核心功能。
- 本产品不是普通 SaaS 后台，也不是普通聊天 Agent。它是高责任 Agent 产品，默认服务薪酬、个税、社保、客户交付、权限、导出和归档等高影响流程。
- AI MUST 优先设计 P0 flows 和 P0 screens。
- AI MUST 把风险分级、最小权限、人工确认、审计追溯、证据闭环、失败停机和 correction run 路径作为界面的一等对象。
- AI MUST 覆盖默认、加载、空、错误、成功、无权限、阻断、高风险、等待人工确认、Agent 降级等关键状态。
- AI MUST 让 Agent 输出以“候选、解释、摘要、追问、证据建议”呈现，不得设计成可静默生效的黑箱自动化。
- AI MUST 保持页面、组件、交互和视觉 tokens 一致。
- AI MUST 将 `docs/design-references/IDNPY-CPV2/Payroll Agent.dc.html` 作为 PC 端高保真视觉与布局参考，但不得把该原型里的静态按钮、假数据或演示状态当成功能验收依据。

## 1. 设计方向

### 1.0 产品形态

| 维度 | 方向 |
|---|---|
| 形态 | 界面型 Web 应用 + 嵌入式对话式业务 Agent |
| 本体 | 主体是客户工作台、AI Intake Inbox、Payroll Run 工作台、变更确认页、证据归档、异常/case 队列、抽屉和确认卡口；Agent 是 run 上下文内的受控建议层 |
| 落地分轨 | 界面型走 §2、§3、§4、§5、§8；嵌入式 Agent 走 §A |
| V1 优先 surface | 桌面 Web 内部运营系统（PC only） |
| 明确不做 | 营销页、员工自助端、移动端适配、移动优先消费 app、自由聊天式 Agent、全自动放行 |

### 1.1 设计目标

让客服/交付专员、算薪人和交付主管能从企业微信文本/截图、Excel、合同和内部备注等原始输入开始，经 AI intake、ChangeProposal 审核、ChangeLedger 沉淀、客户确认包、算薪和正式导出完整推进 payroll run，同时一眼看清风险、缺口、证据、版本、确认人和下一步动作。设计目标不是“漂亮”，是让高影响薪资动作在执行前被理解、被审计、被授权。

### 1.2 产品气质

| 维度 | 方向 |
|---|---|
| 语气 | 冷静、直接、可信、审计友好 |
| 信息密度 | 紧凑专业工具；一屏可扫关键指标、风险和待办，但高影响确认区必须留足阅读空间 |
| 视觉风格 | 内部运营工作台 + 控制台式高责任 Agent |
| 情绪目标 | 用户应感觉“系统不会替我乱做事，但会把该看的证据摆出来” |
| 第一印象 | 可控 |
| 品牌人格 | 穿深灰西装的资深交付负责人：话少、证据足、动作慢半拍但不乱来 |

### 1.3 设计原则

- DP-001: 风险先于效率。阻断项、高风险项、权限缺口、证据失效、规则版本缺失必须比普通数据更显眼。
- DP-002: 预览先于生效。R2+ 动作必须先展示影响范围、差异、理由、操作者权限和可回退路径，再允许确认。
- DP-003: 解释先于信任。每个金额结果、Agent 建议和高风险判断必须能下钻到输入、规则版本、证据和 trace。
- DP-004: Agent 是建议层，不是审批链。Agent 可以生成候选、追问和解释，不能把映射、规则、算薪、锁定、导出、高风险放行静默变成事实。
- DP-005: 表格可以密，判断不能密。列表、文件预览、映射候选可以 compact；确认包、风险放行、导出前预览必须让用户慢下来读。
- DP-006: 审计不是事后日志页。关键页面必须就地显示“谁、何时、基于什么版本、做了什么动作”。
- DP-007: Proposal 和 ledger 必须视觉分离。RawInputItem、ChangeProposal、ChangeLedgerEntry、CustomerConfirmationPack 必须有明确对象状态，不能让用户误以为 AI 候选已经成为事实。

### 1.4 参考与反参考

**正向参考：**

| 参考产品 / 资料 | 可借鉴属性 | 必须保留 |
|---|---|---|
| [ADP Global Payroll](https://www.adp.com/what-we-offer/products/adp-global-payroll.aspx) | 合规 system of record、全局 dashboard、薪资 insight、治理框架 | Yes |
| [Deel Global Payroll](https://www.deel.com/global-payroll/) | 状态、差异、成本在同一 dashboard；审批、角色、控制按地区/实体设置；缺失数据和异常在 approval 前暴露 | Yes |
| [Workday Payroll](https://www.workday.com/en-us/products/payroll/overview.html) | Pay cycle command center、audit summary、results status、AI anomaly detection、trusted audit trails | Yes |
| [OpenAI Agents guardrails](https://openai.github.io/openai-agents-js/guides/guardrails/) / [human-in-the-loop](https://openai.github.io/openai-agents-js/guides/human-in-the-loop/) | guardrails 自动检查，human review 负责敏感动作暂停/批准/拒绝 | Yes |
| [PwC Payroll Controls for Workday](https://www.pwc.com/us/en/technology/alliances/workday-implementation-partners/security-controls/confident-compliance-with-payroll-controls-workday.html) | RBAC、验证规则、异常监控、职责分离、不可变审计轨迹 | Yes |
| `docs/design-references/IDNPY-CPV2/Payroll Agent.dc.html` | Claude Design 生成的 PC 端高保真前端参考：左侧导航、任务台、run 工作台、风险门禁、确认抽屉、审计和 Agent 治理的整体视觉方向 | Yes |

**反参考：**

- 普通聊天框 Agent：把复杂流程塞进对话，用户看不到版本、证据、差异和权限边界。
- 营销型 SaaS 首页：大 hero、渐变装饰、空泛价值主张，对内部交付工具无用。
- Excel 皮肤系统：只复刻表格外观，不建立阻断、高风险、规则版本和 trace。
- 过度 BI 大屏：漂亮图表很多，但不能推进 run、处理证据或执行确认卡口。
- “一键自动算完并导出”：对薪资、税、社保和客户交付是危险设计，V1 禁止。

### 1.5 设计调研摘录

- ADP 强调 global payroll 需要 compliant system of record、governance frameworks、variance insights。
- Deel 强调 one dashboard 跟踪 payroll status、variances、workforce costs，并在 approval 前 flag anomalies 和 missing data。
- Workday 把 payroll 设计成 command center，突出 audit summary、results status、AI anomaly detection 和 trusted audit trails。
- OpenAI Agents 文档把 guardrails 与 human review 分开：自动验证输入/输出/工具行为，敏感动作暂停给人审批。
- PwC payroll controls 强调上游数据验证、角色权限、职责分离、异常监控、审批证据和不可变审计轨迹。

## 2. 信息架构

### 2.1 导航结构

采用左侧主导航 + 顶部上下文 header + 页面内工作台分区。

- 左侧主导航：任务台、AI Intake、Payroll Runs、客户、员工、规则、Agent 治理、归档、审计。
- 顶部 header：当前页面标题、客户/月度/run 状态、系统可用状态、用户角色和全局搜索。
- Payroll Run 详情页内部使用阶段导航 / stepper，而不是把每一步拆成互相孤立的页面。
- 高影响动作统一放在右侧确认抽屉或底部 sticky action bar，避免用户在页面深处误点。
- Agent 入口嵌入 run 详情和具体任务区，默认折叠为建议面板，不作为全局自由聊天入口。

### 2.2 页面清单

| 页面编号 | 页面名称 | 页面目的 | 关联流程 / 需求 | 优先级 |
|---|---|---|---|---|
| SCREEN-001 | 任务台 | 查看待处理 run、阻断、高风险、待确认和逾期任务 | FLOW-001 / REQ-017 | P0 |
| SCREEN-002 | Payroll Run 详情工作台 | 承载 run 全流程、阶段状态、待处理动作和确认卡口 | FLOW-001 / REQ-002 | P0 |
| SCREEN-003 | 原始文件上传与解析预览 | 上传 Excel、展示 workbook/sheet/表头/公式/异常 | FLOW-001/002/003 / REQ-003 | P0 |
| SCREEN-015 | AI Intake Inbox | 汇总企业微信文本/截图、Excel、合同和内部备注，处理归属、重复、证据候选 | FLOW-001/006 / REQ-018 | P0 |
| SCREEN-016 | 变更 Proposal Review | 审核 AI 或人工生成的 ChangeProposal，确认后写入 ChangeLedger | FLOW-001/006 / REQ-019 | P0 |
| SCREEN-004 | 字段映射与标准化确认 | 审核 Agent 映射候选，确认标准化输入和员工匹配 | FLOW-001/002/003 / REQ-004, REQ-005 | P0 |
| SCREEN-005 | 追问与证据中心 | 管理客户追问、证据上传、证据覆盖范围和确认失效 | FLOW-001 / REQ-006 | P0 |
| SCREEN-017 | 客户确认包 | 生成、预览、编辑和回填客户确认包及覆盖范围 | FLOW-001/007 / REQ-020 | P0 |
| SCREEN-006 | 预检查与风险处理 | 处理阻断项、高风险项、预检查建议和放行路径 | FLOW-001 / REQ-007, REQ-010 | P0 |
| SCREEN-007 | 算薪结果与算薪确认包 | 查看摘要、异常、金额、trace，下钻后锁定 | FLOW-001 / REQ-008, REQ-011 | P0 |
| SCREEN-008 | 导出预览与归档 | 预览 workbook/sheet/header/汇总校验，正式导出和归档 | FLOW-001/002/003 / REQ-012, REQ-014 | P0 |
| SCREEN-009 | Correction Run 差异工作台 | 基于已锁定 run 创建更正，展示受影响范围和差额 | FLOW-005 / REQ-013 | P0 |
| SCREEN-010 | 客户与员工主档 | 管理客户配置、员工主档、版本、证据和脱敏字段 | FLOW-001 / REQ-001, REQ-017 | P0 |
| SCREEN-011 | 规则版本与回归发布台 | 起草、审批、发布规则，查看蓝色光标和三福回归结果 | FLOW-004 / REQ-009, REQ-016 | P0 |
| SCREEN-012 | Agent 治理与 Eval | 查看 AgentRun、ToolInvocation、GuardrailResult、EvalRun 和版本发布状态 | FLOW-001/004 / REQ-015A-D | P0 |
| SCREEN-013 | 审计日志 | 查询导出、锁定、放行、敏感明文查看、规则发布等操作 | FLOW-001 / REQ-007, REQ-017 | P0 |
| SCREEN-014 | 发薪与申报证据归档 | 记录发薪状态、付款证据、KS/TK 和 PPh21 申报证据 | FLOW-001 / REQ-014 | P0 |

### 2.3 页面关系

任务台和 AI Intake 是入口，用户进入某个 Payroll Run 后在 Run 详情工作台内沿阶段推进：Intake 归属 -> 文件上传/解析 -> 变更 Proposal Review -> 映射确认 -> 标准化确认 -> 追问/证据 -> 客户确认包 -> 预检查 -> 算薪 -> 算薪确认包 -> 锁定 -> 导出 -> 归档。客户/员工、规则、Agent 治理和审计是横向支撑页，必须能从 run 内通过上下文链接跳入，并带回原 run。

## 3. 页面规格

### SCREEN-001: 任务台

**页面目的：**
让交付团队知道今天该先处理哪个 payroll run，以及哪些任务不能继续推进。

**主要操作：**
筛选并进入待处理 run。

**布局结构：**
1. 顶部区域：标题、全局搜索、角色/授权客户范围、系统状态。
2. 指标区：待处理 run、待 intake 归属、待 proposal 审核、阻断项、高风险项、待客户确认、逾期任务。
3. 主内容区：run 列表表格，默认按阻断/高风险/逾期优先排序。
4. 辅助区：我的待办、最近导出/锁定、Agent 服务状态。

**内容层级：**

| 优先级 | 元素 | 说明 |
|---|---|---|
| 1 | 未归属 intake、待审核 proposal、阻断、高风险、逾期 | 决定处理顺序 |
| 2 | 客户、月份、状态、负责人 | 快速定位 run |
| 3 | 最近动作、Agent 建议可用性 | 辅助判断 |

**使用组件：** CMP-001, CMP-004, CMP-005, CMP-006, CMP-007, CMP-014

**必需状态：**
- 默认：显示指标和 run 表格。
- 加载：指标 skeleton + 表格 skeleton。
- 空状态：说明当前没有待处理 run，并提供“新建 run”入口。
- 错误：展示加载失败原因和重试，不隐藏已有缓存摘要。
- 成功：筛选或新建后 toast 简短确认。
- 无权限：说明当前角色没有客户授权，给出联系管理员路径。
- Agent 降级：Agent 状态显示为不可用，但任务台和人工流程可继续。

**PC 端布局表现：**
- 1200px+：左侧导航 + 指标区 + run 表格，辅助区可下移或折叠。
- 1440px 标准桌面：左侧导航 + 指标区 + run 表格 + 右侧辅助区。
- 1600px+：保留主工作区最大宽度，右侧辅助区可展示更多待办、最近导出/锁定和 Agent 服务状态。

### SCREEN-002: Payroll Run 详情工作台

**页面目的：**
承载 run 的完整操作链，避免文件、映射、证据、算薪、导出散落。

**主要操作：**
按阶段推进 run，并在每个阶段处理待办、阻断和确认。

**布局结构：**
1. 顶部区域：客户、月份、run 状态、风险等级、锁定/导出/归档状态。
2. 阶段导航：Intake、文件、变更、映射、标准化、证据、客户确认、预检查、算薪、确认包、导出、归档。
3. 主任务区：当前阶段的主表格、预览或确认包。
4. 右侧上下文区：阻断/高风险摘要、Agent 建议、最近审计事件。
5. 底部固定操作区：下一步、保存草稿、提交确认、高影响动作入口。

**内容层级：**

| 优先级 | 元素 | 说明 |
|---|---|---|
| 1 | 当前阶段能否继续 | 阻断、缺证据、权限、版本失效必须显眼 |
| 2 | 下一步动作 | 用户要知道现在该做什么 |
| 3 | 历史事件和 Agent 建议 | 可展开查看 |

**使用组件：** CMP-002, CMP-003, CMP-007, CMP-008, CMP-009, CMP-013, CMP-014, CMP-017, CMP-018, CMP-019

**必需状态：**
- 默认：阶段进度、当前任务区、右侧风险摘要齐全。
- 加载：阶段 header 优先加载，主内容 skeleton。
- 空状态：当前阶段无事项时展示清晰下一步。
- 错误：阶段数据失败时阻止高影响动作。
- 成功：阶段推进后展示状态变化和审计事件。
- 无权限：动作按钮禁用并说明所需角色。
- 版本失效：显示失效来源、影响范围、需要回退到的阶段。

**PC 端布局表现：**
- 1200px+：左侧阶段导航 + 主任务区；上下文区可折叠或下移。
- 1440px 标准桌面：左侧阶段导航 + 主任务区 + 右侧上下文区，主任务区不可被 Agent 面板挤压。
- 1600px+：右侧可并排显示风险摘要、Agent 建议和最近审计事件。

### SCREEN-003: 原始文件上传与解析预览

**页面目的：**
从客户原始 Excel 开始保留证据链，并让用户看清文件解析是否可用。

**主要操作：**
上传文件、选择用途、查看解析预览、处理文件阻断。

**布局结构：**
1. 上传区：拖拽上传、用途、月份、来源。
2. 文件版本列表：版本、替代关系、上传人、解析状态。
3. Workbook 预览：sheet 列表、有效区域、表头、样例值、公式/合并单元格提示。
4. 风险区：损坏、超限、缺 sheet、缺表头、宏/外链/指令类文本。

**使用组件：** CMP-005, CMP-006, CMP-007, CMP-008, CMP-012

**必需状态：**
- 默认：文件列表 + 选中文件解析预览。
- 加载：上传进度、解析进度、sheet skeleton。
- 空状态：上传入口和文件用途说明。
- 错误：明确是上传失败、类型不支持、文件损坏还是解析超时。
- 成功：文件版本生成并展示解析摘要。
- 阻断：文件阻断项固定在预览上方，不允许进入映射。

### SCREEN-015: AI Intake Inbox

**页面目的：**
把企业微信文本/截图、Excel、合同、客户确认和内部备注变成可处理的 RawInputItem 队列，而不是散落在聊天和本地文件里。

**主要操作：**
粘贴文本、上传附件、绑定客户/月度/run、处理重复和归属低置信、触发受控 AI 抽取。

**布局结构：**
1. 输入区：粘贴企业微信文本、上传截图/Excel/合同、选择来源渠道和输入类型。
2. Inbox 队列：RawInputItem 状态、客户/月度/run 归属、重复提示、敏感摘要、处理人。
3. 详情预览：原文/附件引用、OCR/解析摘要、脱敏结果、证据候选。
4. 归属与安全区：客户/月度建议、低置信提示、外部指令隔离、待处理 case。

**使用组件：** CMP-005, CMP-006, CMP-007, CMP-008, CMP-013, CMP-014, CMP-017

**必需状态：**
- 默认：队列按未归属、低置信、待抽取、待审核排序。
- 加载：上传、OCR/解析、AI 分类分别展示进度。
- 空状态：说明没有待处理输入，并给出粘贴/上传入口。
- 错误：上传或分类失败不丢原始输入，保留人工绑定路径。
- 无权限：未授权客户的输入不得展示明细，只显示不可处理原因。
- Guardrail：外部文本里的指令被标记为客户输入，不作为系统指令执行。

### SCREEN-016: 变更 Proposal Review

**页面目的：**
把 AI 或人工从 intake 中识别出的变更候选变成可审查、可拒绝、可修改、可确认的 ChangeProposal，再由人工提交到 ChangeLedger。

**主要操作：**
审核 proposal、查看来源证据和差异、修改字段、拆分/合并、拒绝或确认写入 ChangeLedger。

**布局结构：**
1. Proposal 队列：类型、员工/对象、字段、原值/新值、生效期、风险等级、置信档、审核状态。
2. 来源证据区：RawInputItem、截图/文本/Excel 单元格、合同片段和 AgentRun。
3. 差异预览：员工主档、薪酬、入离职、社保/公积金、bonus/扣款、请假/缺勤影响。
4. 审核抽屉：权限、幂等 key、审计摘要、确认后生成的 ChangeLedgerEntry 预览。

**使用组件：** CMP-005, CMP-006, CMP-008, CMP-009, CMP-011, CMP-013, CMP-014, CMP-018

**必需状态：**
- 默认：待审核 proposal 按风险和 payroll run 影响排序。
- 加载：AI 抽取或批量审核时展示 node、数量和可取消状态。
- 空状态：当前 run 没有待审核 proposal，并显示已确认 ledger 摘要。
- 错误：缺证据、权限不足、锁定 run、版本冲突时禁止提交。
- 成功：确认后展示 ChangeLedgerEntry 和审计事件。
- 高风险：关键薪酬字段、离职、税社保基数等变更必须固定显示证据要求和确认人。

### SCREEN-004: 字段映射与标准化确认

**页面目的：**
把 Agent 识别候选转成人工确认的 FieldMappingVersion 和标准化输入。

**主要操作：**
审核映射候选、处理低置信/冲突、确认员工匹配和标准化数据。

**布局结构：**
1. 映射候选表：源 sheet/列、样例值、目标字段、置信档、判断依据。
2. 原始预览联动区：点击候选定位源单元格。
3. 标准化数据预览：员工、组件、金额、币种、证据状态。
4. 冲突处理区：低置信、同员工多行、姓名单独匹配、未匹配员工。

**使用组件：** CMP-005, CMP-006, CMP-007, CMP-008, CMP-009, CMP-011, CMP-015

**必需状态：**
- 默认：按置信档和影响程度排序。
- 加载：Agent 建议生成中展示 node、耗时和可取消。
- 空状态：没有候选时引导人工映射。
- 错误：Agent 失败不阻断人工流程。
- 成功：确认后生成版本并展示审计记录。
- 高风险：低置信确认、新员工、关键字段修改必须提示风险等级和证据要求。

### SCREEN-005: 追问与证据中心

**页面目的：**
把客户确认、企业微信截图/文本、补充文件和追问项统一变成可追溯证据。

**主要操作：**
上传/粘贴证据、关联对象、关闭追问、查看确认覆盖范围。

**布局结构：**
1. 追问清单：缺什么、为什么问、影响对象、截止状态。
2. 证据库：文件/文本/截图/OCR、来源、上传人、确认人、适用月份。
3. 覆盖范围编辑器：run、员工、字段、规则、导出预览版本。
4. 确认失效区：金额或版本变化后旧确认失效提示。

**使用组件：** CMP-006, CMP-007, CMP-008, CMP-009, CMP-013, CMP-015

**必需状态：**
- 默认：追问和证据按阻断影响排序。
- 加载：附件上传和 OCR/解析状态分开显示。
- 空状态：无追问时说明当前证据状态。
- 错误：证据上传失败不得关闭追问。
- 成功：证据关联后显示覆盖范围和审计事件。
- 失效：旧确认不再适用时强制显眼提示。

### SCREEN-017: 客户确认包

**页面目的：**
把本月变更、缺失信息、异常项和需确认事项产品化，减少一线人员在企业微信里反复手写确认，并保留客户确认的版本和证据范围。

**主要操作：**
生成客户确认包草稿、预览建议话术、编辑非关键文案、回填客户回复、绑定确认覆盖范围。

**布局结构：**
1. Pack 概览：版本、生成来源、适用 run 快照、ChangeLedger 版本、状态和失效原因。
2. 分组内容：本月变更、缺失信息、异常项、需客户确认事项、建议话术。
3. 覆盖范围：全 run、部分员工、部分字段、导出预览版本、客户口径。
4. 回填区：企业微信回复、截图、确认文件、确认人、确认时间和 Evidence 链接。
5. 风险区：阻断/高风险/确认失效项固定展示，不允许被普通编辑隐藏。

**使用组件：** CMP-005, CMP-006, CMP-007, CMP-008, CMP-009, CMP-011, CMP-013, CMP-014, CMP-019

**必需状态：**
- 默认：按变更、缺失、异常、需确认事项分组展示。
- 加载：生成草稿、重新计算覆盖范围和回填证据分开显示。
- 空状态：没有可确认事项时说明原因，并提示是否仍需客户确认。
- 错误：生成失败或来源版本不完整时不允许发送给客户。
- 成功：pack 版本生成并可复制建议话术，复制动作记录审计。
- 失效：ChangeLedger、金额、员工范围、银行、税/社保或导出预览变化后强制提示重新生成。

### SCREEN-006: 预检查与风险处理

**页面目的：**
在正式算薪前阻止缺字段、缺证据、规则缺失、权限不明和高风险静默通过。

**主要操作：**
运行预检查、处理阻断项、发起高风险放行请求。

**布局结构：**
1. 风险总览：阻断、核查项、高风险、提示分组统计。
2. 问题列表：影响对象、原因、来源、建议动作、负责人。
3. 放行队列：高风险说明、证据、放行人、职责分离提示。
4. Agent 建议：只解释和建议，不替代系统判定。

**使用组件：** CMP-002, CMP-007, CMP-008, CMP-009, CMP-013, CMP-014

**必需状态：**
- 默认：按阻断 > 高风险 > 核查项 > 提示排序。
- 加载：预检查进度和已完成检查项。
- 空状态：预检查尚未运行时给出运行入口。
- 错误：预检查失败时禁止算薪。
- 成功：阻断清零可进入算薪。
- 等待放行：显示待放行角色和 SLA。

### SCREEN-007: 算薪结果与算薪确认包

**页面目的：**
让算薪人先看摘要和异常，再下钻 trace，确认锁定前不逐格迷路。

**主要操作：**
查看算薪确认包、下钻员工 trace、确认锁定普通 run 或跳转高风险放行。

**布局结构：**
1. 摘要区：人数、应发、实发、PPh21、BPJS、雇主成本、离职/Gross Up/外币人数。
2. 异常区：阻断结果、高风险、环比差异、客户确认包状态、输出校验摘要。
3. 员工结果表：金额、状态、差异、trace 入口。
4. 计算解释链抽屉：输入、组件、规则版本、中间值、取整。
5. 锁定确认抽屉：最后校验清单、权限、审计摘要。

**使用组件：** CMP-004, CMP-005, CMP-007, CMP-009, CMP-010, CMP-011, CMP-014

**必需状态：**
- 默认：摘要优先，不默认展示全量明细。
- 加载：算薪进度、员工批处理状态、可取消规则。
- 空状态：未算薪时展示前置条件。
- 错误：算薪失败或 trace 不完整时阻断锁定。
- 成功：算薪完成后生成确认包版本。
- 高风险未放行：锁定按钮禁用并链接风险清单。

### SCREEN-008: 导出预览与归档

**页面目的：**
在正式导出前让用户看到 workbook/sheet/header/人数/金额/模板结构是否合法。

**主要操作：**
查看导出预览、导出草稿/正式文件、生成归档包。

**布局结构：**
1. 模板选择与状态：蓝色光标、三福、草稿/正式/correction。
2. Workbook 预览：sheet、行列数、必要列、关键汇总。
3. 校验结果：人数、金额、模板结构、客户确认有效性。
4. 导出确认抽屉：文件名、目的、权限、审计记录。
5. 归档包清单：RawInputItem、ChangeLedger、输入文件、映射、规则、客户确认包、算薪确认包、核查报告、导出文件、证据索引。

**使用组件：** CMP-002, CMP-005, CMP-009, CMP-012, CMP-013, CMP-014

**必需状态：**
- 默认：预览先于下载。
- 加载：生成预览/导出文件进度分离。
- 空状态：未锁定时只允许预览/草稿入口。
- 错误：模板结构错误、人数不一致、确认失效必须阻断正式导出。
- 成功：导出后显示归档包生成状态和审计事件。

### SCREEN-009: Correction Run 差异工作台

**页面目的：**
锁定后更正不污染原 run，用户能看清更正范围、原因、差额和证据。

**主要操作：**
创建 correction run、选择受影响员工、重新走预检查/算薪/确认/导出。

**布局结构：**
1. 原 run 快照摘要：锁定时间、导出文件、归档包。
2. 更正范围：员工、字段、原因、证据。
3. 差异表：原值、新值、金额影响、税/BPJS/实发/成本差异。
4. 更正流程 stepper：重新预检查到导出。

**使用组件：** CMP-003, CMP-006, CMP-009, CMP-011, CMP-013

**必需状态：**
- 默认：原 run 只读，更正 run 可编辑。
- 加载：复制快照进度。
- 错误：缺证据或无权限不得创建。
- 成功：创建后进入新 run 工作台。
- 无权限：只有具备角色者可发起。

### SCREEN-010: 客户与员工主档

**页面目的：**
维护客户、员工、配置版本和关键字段证据，同时保证敏感字段默认脱敏。

**主要操作：**
查询客户/员工、维护主档、查看版本差异、绑定证据。

**布局结构：**
1. 搜索与筛选：客户、员工姓名、ID、NIK/护照/NPWP。
2. 主档列表：状态、授权、风险字段、最近版本。
3. 详情抽屉：字段、脱敏值、证据、版本历史。
4. 修改确认：关键字段变更差异、证据、影响 run。

**使用组件：** CMP-005, CMP-006, CMP-009, CMP-011, CMP-013, CMP-015

**必需状态：**
- 默认：敏感字段脱敏显示。
- 加载：列表和详情分离 skeleton。
- 错误：权限或查询失败明确说明。
- 成功：版本生成并展示影响范围。
- 无权限：不返回未授权客户明细。
- 查看明文：必须二次确认目的并记录审计。

### SCREEN-011: 规则版本与回归发布台

**页面目的：**
让规则管理员和负责人以版本方式发布公共规则、客户规则和组件规则。

**主要操作：**
起草规则、跑回归、审批发布、停用旧版本。

**布局结构：**
1. 规则列表：状态、版本、生效月份、适用范围、回归状态。
2. 规则详情：参数、依据、变更说明、客户影响。
3. 回归结果：蓝色光标、三福、失败用例、金额差异。
4. 发布确认：审批人、职责分离、不可编辑提示。

**使用组件：** CMP-005, CMP-007, CMP-009, CMP-011, CMP-013, CMP-016

**必需状态：**
- 默认：按草稿/待审批/已发布/停用分组。
- 加载：回归运行进度。
- 错误：回归失败或权限不足阻断发布。
- 成功：发布新版本并可查看审计。
- 高风险：回归差异超过阈值固定展示。

### SCREEN-012: Agent 治理与 Eval

**页面目的：**
让 Agent 建议可观测、可回归、可停用，不靠“模型应该没问题”上线。

**主要操作：**
查看 AgentRun、AgentStep、ToolInvocation、GuardrailResult、EvalRun 和版本发布门禁。

**布局结构：**
1. Agent 版本概览：prompt/model/tool schema/RAG/memory 版本状态。
2. Eval 结果：critical 通过率、失败用例、阻断原因。
3. Trace 列表：run、node、触发人、输入摘要、输出摘要、成本、耗时。
4. Guardrail 命中：类型、严重级别、处理动作、关联输入。

**使用组件：** CMP-005, CMP-007, CMP-008, CMP-013, CMP-016

**必需状态：**
- 默认：发布门禁状态一眼可见。
- 加载：trace 和 eval 结果分区加载。
- 错误：trace 缺失时对应 Agent 输出不得进入人工确认流程。
- 成功：版本通过门禁后标识 Released。
- Revoked：停用原因和影响范围显眼展示。

### SCREEN-013: 审计日志

**页面目的：**
查询高影响动作、敏感字段访问、导出、锁定、放行、规则发布和 Agent 操作。

**主要操作：**
筛选审计事件、查看详情、追加更正说明。

**布局结构：**
1. 筛选区：客户、run、员工、操作者、角色、动作、时间。
2. 审计表：事件、对象、操作者、角色、时间、结果。
3. 事件详情：输入摘要、输出摘要、版本、证据、IP/设备。
4. 追加说明：不可编辑原日志，只能追加。

**使用组件：** CMP-005, CMP-013, CMP-015

**必需状态：**
- 默认：按时间倒序。
- 加载：表格 skeleton。
- 空状态：当前筛选无事件。
- 错误：审计读取失败不影响原始日志完整性。
- 无权限：普通用户只看授权范围。

### SCREEN-014: 发薪与申报证据归档

**页面目的：**
记录发薪状态、付款证据、BPJS KS/TK、PPh21 BPMP/BPA1 申报证据，不做外部提交。

**主要操作：**
上传证据、关联覆盖范围、查询归档。

**布局结构：**
1. 发薪记录表：员工、实发、币种、状态、发薪日期、付款证据。
2. 申报证据区：KS/TK/PPh21 类型、人数、金额、覆盖范围。
3. 证据关联抽屉：run、员工、批次、导出文件。

**使用组件：** CMP-005, CMP-006, CMP-009, CMP-013

**必需状态：**
- 默认：批次和员工级证据均可查。
- 加载：附件上传和关联分离。
- 错误：证据不改变已锁定算薪结果。
- 成功：证据归档后可从 run 和员工反查。

## 4. 组件规格

### CMP-001: App Shell / 主导航

**用途：** 承载内部系统全局导航、当前角色、系统状态。
**使用位置：** 全部 SCREEN

| 变体 | 用途 |
|---|---|
| Default | 桌面左侧导航 |
| Compact | 窄屏折叠导航 |
| Unauthorized | 无客户授权或角色异常 |

**状态：** 默认、悬停、聚焦、激活、禁用、加载、错误均需可见；导航 active 不只依赖颜色，应有背景/边框/图标状态。

### CMP-002: Risk Gate Banner / 风险门禁条

**用途：** 展示当前页面或 run 是否可继续推进。
**使用位置：** SCREEN-002, 006, 008

| 变体 | 用途 |
|---|---|
| Blocking | 阻断项未清零 |
| HighRisk | 高风险待放行 |
| Warning | 核查项/提示 |
| Ready | 可进入下一步 |

**内容规则：** 必须写清“为什么不能继续”“影响对象”“下一步是谁做”，禁止只写“发生错误”。

### CMP-003: Run Stepper / 阶段进度

**用途：** 展示 run 当前阶段、回退原因、失效范围。
**使用位置：** SCREEN-002, 009

**规则：** 阶段回退必须展示触发原因和时间；已完成阶段可查看快照，不能默默重置。

### CMP-004: Metric Tile / 指标块

**用途：** 展示任务台和确认包关键数字。
**使用位置：** SCREEN-001, 007

**规则：** 指标块不做装饰卡片堆砌。每个指标必须有解释 tooltip 或下钻路径；异常指标用语义色和图标双编码。

### CMP-005: Data Table / 可筛选数据表

**用途：** 承载 run、员工、映射、规则、审计、证据等密集数据。
**使用位置：** 多数 SCREEN

**规则：**
- 默认 compact 行高，但复选框、菜单、错误状态点击区不得缩到难点。
- 支持排序、筛选、列固定、行展开、批量选择。
- 敏感字段默认通过 CMP-015 脱敏。
- 金额列右对齐，ID/版本/状态列短标签化，证据/trace 用链接 chip。

### CMP-006: Evidence Chip / 证据引用

**用途：** 显示证据类型、来源、覆盖范围和有效性。
**使用位置：** SCREEN-003, 004, 005, 007, 008, 009, 014, 015, 016, 017

**变体：** File、Text、Screenshot/OCR、RawInput、ChangeProposal、ChangeLedger、Customer Confirmation、CustomerConfirmationPack、Rule Source、Trace Source、Expired。
**规则：** 证据 chip 必须可点击下钻，不允许只有图标没有来源。

### CMP-007: Issue Row / 阻断与高风险项

**用途：** 统一呈现 blocking issue、high risk issue、核查项和提示。
**使用位置：** SCREEN-001, 002, 004, 005, 006, 007, 011, 012

**规则：**
- 阻断项使用明确红色语义和“Cannot continue”状态。
- 高风险项使用琥珀/橙色语义和“Needs approval”状态。
- 每项必须显示影响对象、来源、处理人、最后更新时间、下一步。

### CMP-008: Agent Suggestion Card / Agent 建议卡

**用途：** 展示 Agent 生成的 intake 分类建议、ChangeProposal、映射候选、追问、证据关联建议、客户确认包草稿、核查解释、确认包摘要。
**使用位置：** SCREEN-002, 004, 005, 006, 012, 015, 016, 017

**规则：**
- 必须显示 node、置信档、引用来源、风险等级、版本、人工处理状态。
- 主操作只能是“采纳为草稿/生成 proposal/发送追问/查看依据/标记不采纳”，不能是“直接生效”。
- 低置信或 guardrail 命中时默认展开原因。

### CMP-009: Approval Drawer / 高影响确认抽屉

**用途：** 承载 R2/R3 动作的预览、差异、权限和确认。
**使用位置：** SCREEN-004, 005, 006, 007, 008, 009, 010, 011, 014, 016, 017

**规则：**
- 展示动作风险等级、影响对象、差异、证据、操作者、所需角色、审计结果。
- 危险动作必须要求用户二次确认，不能只用 toast。
- 禁止把确认按钮放在信息未加载完成的状态。

### CMP-010: Calculation Trace Panel / 计算解释链

**用途：** 解释员工级金额结果。
**使用位置：** SCREEN-007

**规则：** 按“结果 -> 输入 -> 组件 -> 规则版本 -> 中间值 -> 取整 -> 来源”展开。默认只展示摘要，下钻逐层展开。

### CMP-011: Diff Viewer / 差异查看器

**用途：** 展示 ChangeProposal、ChangeLedger、员工主档、标准化数据、客户确认包版本、规则、correction run、导出预览的差异。
**使用位置：** SCREEN-004, 009, 010, 011, 016, 017

**规则：** 增删改用颜色 + 文本标签双编码；金额差异必须显示绝对值和百分比。

### CMP-012: Workbook Preview / Excel 预览

**用途：** 预览原始 Excel 和导出 workbook。
**使用位置：** SCREEN-003, 008

**规则：** 保留 sheet、行列、表头、公式文本、显示值、合并单元格提示；只读，不在线编辑原始 Excel。

### CMP-013: Audit Timeline / 审计时间线

**用途：** 在上下文中展示关键动作，不把审计藏到最后。
**使用位置：** SCREEN-002, 005, 007, 008, 009, 010, 011, 012, 013, 014, 015, 016, 017

**规则：** 审计事件不可编辑；更正只能追加说明；Agent 和人类动作必须可区分。

### CMP-014: Role-Sensitive Action / 角色敏感动作按钮

**用途：** 根据角色、客户授权、run 状态和风险等级控制动作。
**使用位置：** 多数 SCREEN

**规则：** 禁用按钮必须说明缺什么权限或前置条件，不允许灰掉不解释。

### CMP-015: Sensitive Field Mask / 敏感字段脱敏

**用途：** 默认脱敏银行账号、证件号、NPWP 等字段。
**使用位置：** SCREEN-004, 010, 013

**规则：** 查看明文必须说明目的、确认权限并记录审计；复制明文也算敏感访问。

### CMP-016: Eval Result Badge / Eval 结果徽标

**用途：** 标记 Agent、规则或工具版本是否通过 critical eval / regression。
**使用位置：** SCREEN-011, 012

**规则：** Draft、Internal Test、Released、Deprecated、Revoked 必须视觉区分；critical 失败不得用中性灰隐藏。

### CMP-017: Intake Item Row / 原始输入行

**用途：** 展示 RawInputItem 的来源、类型、归属、脱敏摘要、处理状态和下一步。
**使用位置：** SCREEN-002, 015

**变体：** WeCom Text、Screenshot/OCR、Excel、Contract、Customer Confirmation、Internal Note、Unassigned、Duplicate Risk、Guardrail Flagged。

**规则：**
- 必须显示客户/月度/run 归属状态；未归属时不能出现“生成变更”主操作。
- 原文默认折叠并脱敏；查看完整原文或附件必须走权限和审计。
- 外部文本中的指令类内容必须用安全标识说明“仅作为客户输入保存”。

### CMP-018: Change Proposal Row / 变更候选行

**用途：** 展示 ChangeProposal 的业务含义、差异、证据、风险和审核动作。
**使用位置：** SCREEN-002, 016

**变体：** Pending Review、Needs Evidence、Low Confidence、Approved、Rejected、Changed Before Commit、Committed。

**规则：**
- Proposal 状态必须和 ChangeLedgerEntry 状态视觉分离，不能只靠颜色区分。
- 每行必须展示影响对象、字段、原值/新值、生效期、风险等级、置信档、来源证据和审核人。
- 提交到 ChangeLedger 前必须通过 Approval Drawer，展示幂等 key、审计摘要和锁定 run 保护。

### CMP-019: Confirmation Pack Builder / 客户确认包编辑器

**用途：** 生成和编辑 CustomerConfirmationPack，展示分组事项、建议话术、覆盖范围和回填证据。
**使用位置：** SCREEN-002, 017

**变体：** Draft、Ready to Send、Awaiting Reply、Partially Confirmed、Confirmed、Expired、Blocked。

**规则：**
- 本月变更、缺失信息、异常项、需确认事项必须分组展示，并保留来源对象链接。
- 阻断、高风险、确认失效和关键缺失项不能被普通编辑隐藏；需要更高权限和审计理由。
- 客户回复必须绑定 pack 版本和覆盖范围，不能用一句“确认无误”覆盖所有对象。

## 5. 视觉系统

### 5.1 色彩变量

V1 默认浅色主题。不要继续扩大现有米色/暖灰占比，避免内部高责任工具看起来像柔和记事本。基底改为冷中性灰白，保留 teal 作为主操作和系统可用状态，增加蓝色信息、琥珀风险、红色阻断、紫色 Agent 治理的语义层。

| 变量 | 用途 | 值 / 方向 |
|---|---|---|
| color.background | 页面背景 | #F7F8FA |
| color.surface | 主面板、卡片、表格 | #FFFFFF |
| color.surface.subtle | 次级区、空状态 | #F1F4F6 |
| color.sidebar | 主导航 | #E8ECEF 或更冷的浅灰 |
| color.primary | 主操作、可继续状态 | #0F766E |
| color.primary.strong | 主操作悬停/选中 | #0B5F59 |
| color.info | 信息、链接、trace | #2563EB |
| color.warning | 高风险、待放行 | #B45309 |
| color.danger | 阻断、危险动作 | #B42318 |
| color.agent | Agent 建议、Eval、治理 | #6D28D9，低占比使用 |
| color.success | 成功、已清零 | #15803D |
| color.text.primary | 主文本 | #111827 |
| color.text.secondary | 次级文本 | #4B5563 |
| color.text.muted | 辅助说明 | #6B7280 |
| color.border | 分割线、边框 | #D7DEE5 |
| color.focus | 键盘焦点 | #2563EB |

### 5.2 字体排版

| 变量 | 用途 | 字号 / 字重 / 行高 |
|---|---|---|
| text.page.title | 页面标题 | 24px / 700 / 32px |
| text.section.title | 区块标题 | 16px / 650 / 24px |
| text.table.header | 表头 | 12px / 650 / 18px，字母可大写，中文不加字距 |
| text.body | 正文 | 14px / 400 / 22px |
| text.body.strong | 重点正文 | 14px / 600 / 22px |
| text.caption | 辅助说明 | 12px / 400 / 18px |
| text.amount | 金额 | 14px / 600 / 22px，tabular-nums |
| text.code | ID、版本、trace、schema | 12-13px / 500 / 18px，ui-monospace |

**排版规则：**
- 中文界面为主，英文/印尼语字段作为别名或 secondary label。
- 金额、版本、ID 使用等宽或 tabular number。
- 不使用负字距；不使用 viewport width 缩放字体。
- 表格内标题和标签保持紧凑，确认抽屉和风险说明保持更松行高。

### 5.3 间距系统

| 变量 | 用途 | 值 |
|---|---|---|
| space.2xs | 表格内紧密元素 | 4px |
| space.xs | 图标与文字、chip 间距 | 8px |
| space.sm | 表单、按钮内部 | 12px |
| space.md | 面板内边距 | 16px |
| space.lg | 页面区块间距 | 24px |
| space.xl | 页面主分区 | 32px |

**规则：**
- 数据密集区使用 4px 步进。
- 高影响确认区最小内边距 20px，不追求极限压缩。
- 表格、工具栏、固定操作区要有稳定高度，动态内容不得造成布局跳动。

### 5.4 形状与层级

| 变量 | 用途 | 值 / 方向 |
|---|---|---|
| radius.sm | chip、tag、小按钮 | 4px |
| radius.md | 输入框、表格容器、导航项 | 6px |
| radius.lg | 抽屉、弹窗、大面板 | 8px |
| shadow.sm | 悬浮工具栏 | 0 1px 2px rgba(17, 24, 39, 0.08) |
| shadow.md | 抽屉、弹窗 | 0 12px 32px rgba(17, 24, 39, 0.16) |

**规则：**
- 卡片圆角不超过 8px。
- 不使用装饰性渐变、发光球、bokeh、漂浮卡片套卡片。
- 层级主要靠边框、留白和 sticky 区分，阴影只用于抽屉、弹窗、浮层。

### 5.5 图标与图像风格

- 使用 lucide-react 图标，线宽 1.75-2。
- 图标必须服务状态、动作或对象类型，不做装饰。
- 空状态不使用大插画；用小图标 + 原因 + 下一步。
- 不使用真实人物照片、营销插图或抽象 AI 光效。

## 6. 交互与反馈规则

### 6.1 全局交互规则

| 模式 | 规则 |
|---|---|
| 主操作 | 每页最多一个主操作，放在 header、工具栏或 sticky action bar |
| 次要操作 | 使用文字按钮或 icon+tooltip，避免和主操作抢层级 |
| 危险操作 | 使用 Approval Drawer，展示影响范围、差异、证据、权限和审计 |
| 保存行为 | 低风险草稿可自动保存；R2+ 生效必须手动确认并版本化 |
| 导航 | 离开有未保存高影响草稿时必须提示 |
| 弹窗 / 抽屉 | 抽屉用于详情、证据、trace、确认；弹窗只用于轻量确认或不可继续错误 |
| 批量操作 | 必须显示选中数量、影响对象、失败项和可重试项 |
| 权限 | 无权限动作保留可见但禁用，并说明所需角色 |

### 6.2 风险动作分级交互

| 风险等级 | UI 行为 | 示例 |
|---|---|---|
| R0 基础设施 | 可直接执行，失败 toast + 日志 | 切换筛选、查看详情 |
| R1 敏感数据读写 | 默认脱敏，明文查看需目的确认和审计 | 查看银行账号、NPWP |
| R2 生效配置 | 预览差异 + 人工确认 + 版本化 | 字段映射生效、员工主档版本 |
| R3 高影响业务动作 | 前置校验 + 角色确认 + 审计 + 不可逆提示 | 正式算薪、锁定、正式导出、规则发布 |
| R4 外部提交/付款 | V1 不实现，只归档证据 | 银行付款、政府平台提交 |

### 6.3 反馈规则

| 场景 | UI 反馈 | 文案规则 |
|---|---|---|
| 成功 | 小 toast + 审计时间线追加 | 写清生成的版本或对象 |
| 错误 | 就地错误 + 页面级风险条 | 写清原因、影响、下一步 |
| 警告 | 琥珀色风险条或 issue row | 写清是否阻断 |
| 空状态 | 小图标 + 原因 + 下一步 | 禁止鼓励式废话 |
| 加载 | skeleton、进度、已完成步骤 | 长任务显示具体 node 或批次 |
| 无权限 | 禁用态 + 所需角色 + 申请路径 | 不泄露未授权数据 |
| Agent 降级 | Agent 面板提示不可用，人工流程保留 | 不把 AI 不可用写成系统不可用 |

### 6.4 动效规则

- 动效必须快、短、功能性；用于抽屉进入、行展开、状态切换、进度更新。
- 不使用表现性动效、粒子、光效、打字机式长文本装饰。
- 必须支持 reduced motion。
- 高影响确认不使用夸张成功动画，避免误导用户以为“已经全部安全”。

## 7. UX 文案与内容规则

### 7.1 文案语气

清晰、直接、证据导向。不要“智能为您自动完成”这类吹嘘。Agent 文案必须把不确定性说出来。

### 7.2 按钮文案规则

- 主按钮使用动词 + 对象：`运行预检查`、`确认映射版本`、`生成导出预览`。
- R3 动作必须明确正式性：`确认锁定 run`、`正式导出文件`、`发布规则版本`。
- 危险按钮使用具体后果：`作废此 run`，不要写模糊的 `删除`。
- Agent 建议按钮使用低承诺措辞：`采纳为草稿`、`查看依据`、`标记不采纳`。

### 7.3 表单文案规则

- label 写业务字段名，必要时附英文/印尼语别名。
- placeholder 不替代 label。
- helper text 说明证据要求、格式和影响范围。
- error message 写“字段 + 问题 + 处理方式”，例如：`汇率缺少客户确认证据，不能进入预检查。上传证据或选择已确认汇率版本。`

### 7.4 空状态文案规则

- 必须解释为什么为空。
- 必须给下一步操作或等待条件。
- 不写“暂无数据哦”这种轻佻语气。

### 7.5 Agent 文案规则

- Agent 不能说“我已完成生效”，只能说“我生成了候选/建议/摘要，等待你确认”。
- 低置信必须明确标注，不能藏在 tooltip。
- RAG 无来源必须说“不确定 / 需人工确认”。
- Guardrail 命中必须说“该文本作为客户输入保存，不作为系统指令执行”。

## 8. 可访问性与 PC 端布局要求

### 8.1 可访问性目标

目标为 WCAG 2.2 AA。P0 先覆盖键盘操作、焦点可见、语义标题、表单 label、错误关联、颜色非唯一表达、对比度和 reduced motion。

### 8.2 必需可访问性规则

- [ ] 所有可交互元素可通过键盘访问。
- [ ] 焦点状态清晰可见。
- [ ] 表单字段有明确 label。
- [ ] 错误信息与对应字段关联。
- [ ] 不只依赖颜色表达状态。
- [ ] 正文和背景有足够对比度。
- [ ] 图标有合适的替代文本或被标记为装饰性。
- [ ] 页面标题和区块标题结构清晰。
- [ ] 表格支持键盘导航、排序状态朗读和行展开说明。
- [ ] 高影响确认抽屉打开后焦点锁定在抽屉内，关闭后回到触发按钮。

### 8.3 PC 端布局规则

| 断点 / 设备 | 规则 |
|---|---|
| PC 最小验收 1200px+ | 左侧导航 + 主工作区；复杂表格允许区域内横向滚动 |
| 标准桌面 1440px | 左侧导航 + 主工作区 + 右侧风险/Agent/审计上下文区，作为 V1 主要验收视口 |
| 宽屏 1600px+ | 主工作区最大宽度约束，右侧可并排显示 Agent、风险和审计面板 |

**规则：**
- V1 不做移动端和平板端适配，不以 375px、768px 等移动/平板视口作为验收条件。
- 页面可以设置 PC 工作台最小宽度；低于最小宽度时可提示“请在桌面端使用”，不需要移动端重排。
- 固定工具栏、表格列、按钮、状态标签必须有稳定尺寸，动态文本不得撑坏布局。
- 高影响确认在 PC 端使用右侧抽屉，必须保留焦点锁定、二次确认、权限说明、影响范围、差异和审计提示。

## A. 嵌入式高责任 Agent 规格

### A.1 交互骨架与模式

| 维度 | 方向 |
|---|---|
| 驱动方式 | 用户点击业务任务触发为主；自然语言只作为 run 内补充提问，不做全局万能输入框 |
| 默认模式 | 只读建议模式：Agent 可生成 intake 分类、ChangeProposal、解释、追问、客户确认包草稿和摘要 |
| 可选模式 | Intake 分类、变更 proposal 抽取、映射建议、追问建议、客户确认包草稿、核查解释、确认包摘要、资料库问答、trace 查看 |
| 禁止模式 | 自动写入 ChangeLedger、自动生效、自动放行、自动锁定、自动导出、自动改规则、自动发送客户正式结论 |
| 一轮交互 | 用户触发任务 -> Agent 声明读取范围 -> 运行 node/tool -> 输出建议卡/候选对象 -> 用户查看依据 -> 用户采纳为草稿、审核提交或拒绝 -> 系统记录人工结果 |

### A.2 呈现单元与技术内容

| 呈现单元 | 呈现方向 |
|---|---|
| 用户消息 | 绑定 run 和当前阶段，不作为跨客户自由上下文 |
| Agent 回复 | 结构化卡片优先，少量自然语言解释 |
| 工具调用 | 可折叠块，展示工具名、版本、输入摘要、输出摘要、耗时、状态 |
| RawInput 摘要 | 来源渠道、输入类型、客户/月度归属建议、重复提示、敏感字段提示 |
| ChangeProposal | 影响对象、字段、原值/新值、生效期、证据、风险等级、置信档、审核状态 |
| 映射候选 | 表格行 + 置信档 + 来源单元格 + 判断依据 |
| 追问建议 | 追问对象、理由、影响范围、建议文案 |
| 客户确认包草稿 | 本月变更、缺失信息、异常项、需确认事项、建议话术、覆盖范围 |
| 核查解释 | 引用 calculation trace、规则版本和证据 |
| 计划 / 待办 | 只展示业务步骤和完成状态，不展示隐藏推理 |
| 错误 / guardrail | 与正常输出分层显示，严重命中固定在卡片顶部 |

### A.3 Agent 运行态

| 运行态 | 显示方向 |
|---|---|
| 待读取上下文 | 显示将读取的对象范围、脱敏策略和最大行数/单元格数 |
| 流式输出中 | 对自然语言摘要可流式；表格候选等结构化结果等待完整后渲染 |
| 跑工具中 | 显示 node、工具名、状态和耗时，不显示敏感参数明文 |
| 等待授权 | 只用于人工采纳、proposal 审核、ChangeLedger 写入、客户确认包发送/回填、证据关联、R2/R3 确认，不用于 Agent 自己请求越权 |
| 被中断 | 显示已完成步骤、未完成步骤、是否生成了任何候选对象 |
| 长任务进度 | 阶段进度 + 最近工具调用 + 可取消；超过阈值提示走人工流程 |
| 服务降级 | Agent 建议不可用，人工映射/预检查/算薪/导出继续可用 |

### A.4 透明度与授权

| 维度 | 规则方向 |
|---|---|
| 推理展示 | 不展示隐藏推理；展示可审计依据：输入引用、规则版本、证据、置信、风险 |
| 动作可回溯 | 每次 AgentRun、AgentStep、ToolInvocation、GuardrailResult 都有 trace 链接 |
| 授权粒度 | intake 归属确认、proposal 采纳、ChangeLedger 写入、客户确认包发送/回填、证据关联、映射生效、规则发布、锁定、导出分别授权 |
| 动作分级 | R0 可自动；R1 脱敏/审计；R2/R3 必须人工确认；R4 V1 禁止执行 |
| 危险动作拦截 | Agent 不得执行 ChangeLedger 写入、锁定、正式导出、高风险放行、规则发布、外部提交或客户正式发送 |
| 可中断 | 长任务可中断；中断后展示哪些候选未生效 |
| 证据边界 | 客户文件、截图、企业微信文本、RAG 内容都按不可信输入处理 |

### A.5 多 surface

| 维度 | 方向 |
|---|---|
| V1 surface | Web 内嵌 Agent 面板 |
| 终端形态 | N/A |
| IDE 扩展 | N/A |
| 未来多端 | V1 不做；未来若增加管理端/审核端，不得弱化审批、审计和 trace |
| 一致性 | 风险等级、置信档、授权、trace、证据引用在所有 surface 必须一致 |
| 可变边界 | Web PC 端展示完整表格、抽屉、上下文侧栏和高影响确认链路 |

## 9. 实现说明

- 当前项目为 Next.js 16、React 19、TypeScript、CSS Modules、lucide-react。
- V1 继续使用 CSS Modules + CSS variables，不先引入大型 UI 组件库，避免和高责任交互细节打架。
- 已有代码使用 teal accent 和暖中性背景；后续实现应把背景调向冷中性灰白，避免一屏读成单一米色/棕色系。
- 图标使用 lucide-react，按钮内优先用图标 + tooltip。
- 不做 landing page，不做 hero，不做营销图。
- 不使用卡片套卡片；页面主结构用 full-width bands 或 unframed workbench，卡片只给重复条目、指标块、弹窗和确实需要框住的工具。
- 暗色模式 V1 标为 [待确认]；默认浅色优先，因为薪资确认和 Excel 预览更需要打印/审计友好的明亮界面。
- `docs/design-references/IDNPY-CPV2/Payroll Agent.dc.html` 是 PC 端高保真参考，开发时可借鉴其信息密度、左侧导航、任务台、风险门禁、Issue Row、Approval Drawer、Trace 和 Agent 治理呈现；但必须补齐真实权限、状态机、阻断/高风险 fail closed、审计写入和错误/无权限状态。
- 设计稿若后续进入 Figma，应优先画 SCREEN-002、SCREEN-015、SCREEN-016、SCREEN-017、SCREEN-004、SCREEN-006、SCREEN-007、SCREEN-008、SCREEN-012 这 9 个高风险核心屏。

## 10. 假设与待确认问题

### 10.1 假设

| 编号 | 假设 | 假设依据 | 错误风险 |
|---|---|---|---|
| DASM-001 | V1 是内部 PC 桌面 Web 工作台 | Product Spec、已有 Next.js 骨架和用户确认均指向 PC 端内部系统 | 若未来要移动端或平板端，需要单独重做导航、表格和高影响确认体验 |
| DASM-002 | 没有现成品牌资产必须沿用 | 当前代码仅有 `ID` brand mark 和 teal accent | 若有企业 VI，色彩和 logo 需调整 |
| DASM-003 | 中文为主，英文/印尼语作为字段别名 | Product Spec 明确 V1 中文界面，字段字典支持多语别名 | 若交付团队主语言变化，文案和布局长度需复审 |
| DASM-004 | Agent 以嵌入式面板呈现，不做独立聊天页 | 高责任 Agent 需要绑定 run、证据和权限 | 若改独立 Agent 页，容易丢失上下文和控制边界 |
| DASM-005 | 浅色主题优先，暗色模式后置 | 审计、表格、Excel 预览、导出确认更适合浅色 | 若用户长时间夜间操作，需增加暗色主题 |

### 10.2 待确认问题

| 编号 | 问题 | 是否阻塞 | 备注 |
|---|---|---:|---|
| DQ-001 | 是否有公司 logo、品牌色或内部系统视觉规范必须沿用？ | No | 没有则按本文 token 执行 |
| DQ-002 | 导出预览是否需要像 Excel 一样保留样式，还是只验证结构和关键字段？ | Yes | 影响 SCREEN-008 视觉和导出测试成本 |
| DQ-003 | 高风险放行是否需要双人审批，还是单个算薪负责人/交付主管即可？ | No | 影响 Approval Drawer 和审计文案 |
| DQ-004 | 暗色模式是否进入 V1？ | No | 默认不进入 |
| DQ-005 | Agent trace 是否面向业务用户全部可见，还是只给管理员/审计角色？ | No | 影响 SCREEN-012 权限和信息密度 |
| DQ-006 | 移动端和平板端是否进入 V1 验收？ | No | 已确认不进入；V1 只做 PC 端 |

## 11. 覆盖清单

- [x] Product 形态：界面型 Web + 嵌入式高责任 Agent。
- [x] P0 页面：覆盖 Product Spec 的 FLOW-001 至 FLOW-007、REQ-001 至 REQ-020。
- [x] 高责任原则：风险分级、最小权限、人工确认、审计追溯、证据闭环、失败停机、correction run。
- [x] Agent 边界：建议层、intake/proposal/pack 草稿、trace、guardrails、human review、eval、版本治理。
- [x] 核心状态：默认、加载、空、错误、成功、无权限、阻断、高风险、等待确认、Agent 降级。
- [x] 视觉系统：色彩、字体、间距、圆角、阴影、图标。
- [x] 交互系统：R0-R4 风险动作、确认抽屉、敏感字段脱敏、导出预览。
- [x] 可访问性：WCAG 2.2 AA 方向、键盘、焦点、表单、错误、颜色非唯一表达。
- [x] PC 端布局：1200px+ 最小工作台、1440px 标准桌面、1600px+ 宽屏；移动端和平板端不进入 V1 验收。
